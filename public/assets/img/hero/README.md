Drop your hero slideshow photos here, named to match `PortalController::heroSlides()`:

- hero-1.jpg
- hero-2.jpg
- hero-3.jpg

If a file is missing, the page falls back to a plain gradient panel instead of a broken image icon, so the site still looks fine without real photos.
